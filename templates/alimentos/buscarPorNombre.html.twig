{% extends 'alimentos/layout.html.twig' %}

{% block contenido %}

	<form name="formBusqueda" action="{{ path('app_alimentos_buscarPorNombre') }}" method="POST">
		<table>
			<tr>
				<td>nombre alimento:</td>
				<td><input type="text" name="nombre" value="{{ nombre }}">(puedes utilizar '%' como comodín)</td>

				<td><input type="submit" value="buscar"></td>
			</tr>
		</table>
	</form>

	{% if resultado %}
		{% include 'alimentos/tablaAlimentos.html.twig' with {'alimentos': resultado} %}
	{% endif %}

{% endblock %}
